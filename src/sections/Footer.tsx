import React from "react";
import styles from "../styles/Footer.module.css";

const FooterSection = () => {
  return (
    <footer id="DSN Decors Footer" className={styles.footerMainContainer}>
      FooterSection
    </footer>
  );
};

export default FooterSection;
